import { defender, ranges } from './constants'
import Game from './Game'
import Hero from './Hero'
import { computeDistance, random } from './utils'

export const movePusher = (game: Game, hero: Hero) => {
  // Go towards enemy base
  // Find a spider
  // Place between the spider and the enemy base
  // WIND WIND WIND
}
